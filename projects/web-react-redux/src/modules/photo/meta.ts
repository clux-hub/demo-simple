import {RouteParams} from './entity';

const defaultRouteParams: RouteParams = {
  listSearch$: {
    pageCurrent: 1,
  },
  listView: '',
  itemId$: '',
  itemView: '',
};

export default defaultRouteParams;
